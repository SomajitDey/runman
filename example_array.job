# Example .job file for array jobs
# Runman sets RUNMAN_ARRAY_INDEX for each elemental job
# Runman also suffixes the given stdout path with the array index, for storing stdout of the corresponding elemental job

cmd="./example_array.sh"
comment="Example array job"
stdout="example_array.out"
# Replicate the given job for indices: 1, 3, 5.
array=1-7:2

